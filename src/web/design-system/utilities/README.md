# Utilities layer

This layer is reserved for business-neutral interaction utilities required by production design-system APIs. It intentionally contains no public utility today. Add a utility only with a production consumer, focused tests, and an explicit decision about whether it remains private or joins the root public API.
