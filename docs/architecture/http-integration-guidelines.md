# HTTP Integration Guidelines

Typed HttpClient, service discovery, explicit timeout, cancellation, tracing/correlation, stable contracts, retry only safe operations, idempotency keys where warranted, and no deep synchronous call chains.
