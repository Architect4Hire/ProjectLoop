# Security Design

Default deny; ASP.NET Core Identity; explicit tenant/project authorization; private Blob; managed identity/Key Vault; input/file validation; anti-forgery/CORS policies appropriate to deployment; no secrets/SAS/document bodies in logs; privileged actions audited.
