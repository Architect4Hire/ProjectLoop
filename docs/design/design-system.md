# Project Loop Design System

The local design system under `src/web/design-system/` owns semantic tokens, typography, spacing, forms, buttons, navigation, shells, tables, statuses, timelines, document cards/lists, approval banners/actions, dialogs, drawers, empty/loading/error states and Tailwind recipes. Feature code composes these primitives.
