# Testing Strategy

Prioritize domain transition tests, cross-tenant denial, authorization matrices, repository scoping, Blob authorization, immutable document versions, stale-version approval attempts, outbox atomicity, duplicate event delivery, inbox behavior, dead-letter/failure paths and Angular loading/error/empty/success flows.
