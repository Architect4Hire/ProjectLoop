# Observability Design

OpenTelemetry traces Angular->YARP->service->SQL/Redis/Blob and outbox->Service Bus->consumer/inbox. Metrics cover HTTP latency/errors, cache hit/miss, outbox age, message failures, duplicate consumption, notification delivery and blob operations.
