# Audit Design

Audit stores append-oriented facts for identity changes, tenant membership, document publication/access where required, approval requests/decisions and privileged operations. Capture actor, tenant, resource, action, UTC time and correlation. Avoid content payload dumping.
