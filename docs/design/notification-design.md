# Notification Design

Notifications consume domain events and own template rendering, delivery attempts, retry/backoff and final failure/dead-letter visibility. They do not own approval or project truth.
