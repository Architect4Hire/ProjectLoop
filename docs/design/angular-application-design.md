# Angular Application Design

Angular 22 standalone, lazy feature routes, strict TypeScript, signals for view state, typed reactive forms, built-in control flow and accessible semantic HTML. Client features should align to dashboard, documents, approvals and project areas without mirroring backend services mechanically.
