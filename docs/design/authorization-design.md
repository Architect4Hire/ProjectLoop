# Authorization Design

Resource authorization combines authenticated user, tenant membership, project membership, role/policy, visibility and resource state. Document content access is authorized before Blob access. UI guards are convenience only.
