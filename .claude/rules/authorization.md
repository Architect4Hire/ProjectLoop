# Authorization Rule

Authorize server-side using authenticated identity plus tenant/project membership, role/policy, resource visibility and state. UI guards are not security controls.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
