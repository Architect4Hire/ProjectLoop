# Observability Rule

Trace requests across gateway, services, SQL, Redis, Blob, Service Bus and consumers. Use structured logs, metrics and correlation IDs. Never log secrets or document bodies.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
