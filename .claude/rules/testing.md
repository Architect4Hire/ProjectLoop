# Testing Rule

Test domain rules, tenant isolation, authorization, persistence, idempotency, event contracts, integration boundaries and critical Angular flows. Architectural invariants deserve tests.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
