# Approvals Rule

Approval requests and decisions are explicit state machines and immutable business history. Capture who, what version, decision, when, comments and correlation/audit metadata.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
