# .NET Rule

Target .NET 10. Use nullable reference types, cancellation, async I/O, DI, options, structured logging, ProblemDetails, and service-owned abstractions.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
