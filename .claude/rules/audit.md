# Audit Rule

Create explicit append-oriented audit facts for security-sensitive and business-significant actions. Logs are diagnostic; they are not the business audit ledger.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
