# Messaging Rule

Use Service Bus for completed business facts, fan-out, independent retry, eventual consistency and durable workflows. Events are past-tense, versioned and idempotently consumable.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
