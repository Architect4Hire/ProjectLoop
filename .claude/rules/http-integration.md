# HTTP Integration Rule

Use typed HttpClient for immediate queries and short commands. Propagate cancellation/tracing, define timeouts, avoid deep sync chains, and do not fake cross-service atomicity.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
