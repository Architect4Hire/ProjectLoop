# Angular 22 Rule

Use standalone APIs, signals, strict typing, built-in control flow, lazy routes, typed forms, accessibility, and zoneless-compatible patterns. No React vocabulary or patterns.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
