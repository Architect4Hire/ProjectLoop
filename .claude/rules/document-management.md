# Document Management Rule

Documents are first-class resources with type, status, visibility, project/tenant ownership, current version, history and audit. Do not model them as filename+URL attachments.

## Required checks
- Follow the root `CLAUDE.md`.
- Preserve service ownership.
- Add or update tests for changed behavior.
- If a change establishes a new architectural convention, create/update an ADR first.
